#OPERAÇÕES
import math
n = int(input('Digite um número: '))
dob = n * 2
trip = n * 3
raiz = math.sqrt(n)
print('O dobro desse número é {0}, o triplo dele é {1} e a raíz quadrada dele é {2:.0f}.'.format(dob, trip, raiz))
#coloquei esse :.0f para ser uma raíz quadrada inteira.